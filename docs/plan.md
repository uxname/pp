## План разработки проекта

### Цель
- Реализовать команду `--version`, которая предоставляет текущую версию CLI.

### Задача
- Определить место в архитектуре для хранения версии, добавить флаг `--version`, вернуть читаемый вывод и убедиться, что команда работает в production-сборке.
