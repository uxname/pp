## 1. Implementation
- [x] 1.1 Расширить `CleanerService.collectCommentRanges`, чтобы он распознавал не только JS-комментарии, но и блоки `/* ... */`, любые встроенные HTML-комментарии и другие языковые конструкции.
- [x] 1.2 Убедиться, что `CleanerService.shouldRemove` по-прежнему уважает `keepJSDoc` и набор встроенных/пользовательских подпараметров.
- [x] 1.3 Ограничить удаление HTML-комментариев `.html`/`.htm` файлами, чтобы строки с регулярными выражениями в других файлах не удалялись по ошибке.
- [x] 1.4 Обновить документацию команды (описание/help `clean`), чтобы упомянуть новые типы комментариев и их ограничения.
- [x] 1.5 Добавить флаг `--changed` (`-c`), чтобы `clean` мог работать только с файлами из Git-статуса, и логировать `Нет изменённых файлов для очистки`, если фильтр пуст.

## 2. Validation
- [x] 2.1 Запустить `npm run lint`, чтобы убедиться, что новая логика не нарушила существующее поведение.
